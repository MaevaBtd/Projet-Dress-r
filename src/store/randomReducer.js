export const initialState = {
 
};

// == Action Type



// Reducer
const randomReducer = (state = initialState, action = {}) => {
  switch (action.type) {
 
    default:

      return state;
  }
};

// == Action Creator



export default randomReducer;
