 
 
 {% block nav %}
 <nav class="navbar navbar-expand-lg navbar-dark bg-primary">

            <a class="navbar-brand" href="{{path('backoffice_index_users')}}">Dress'R</a>
            <div class="collapse navbar-collapse" id="navbarColor02">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('backoffice_index_users')}}">
                            
                            Users
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('backoffice_index_styles')}}">
                            
                            Styles
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('backoffice_index_types')}}">
                            
                            Types
                        </a>
                    </li>
            </form>
        </div>
    </nav>
    {# On demande à la variable globale APP d'acceder aux flashes #}
    {% for label, messages in app.flashes %}
        <div class="container mt-4">
            <div class="alert alert-{{ label }}">
                {% for message in messages %}
                    <p>{{ message }}</p>
                {% endfor %}
            </div>
        </div>
    {% endfor %}

{% endblock %}
